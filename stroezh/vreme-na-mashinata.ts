/**
 * КОЛКО ЧАКА ТЕСТ, КОЙТО ПУСКА МАШИНА · един дом на числото (правило 14).
 *
 * Тестовете на портите не викат машините като функции — те ги ПУСКАТ като
 * процеси (`spawnSync(node, …)`), защото точно така ги пуска и човекът, и
 * куката, и CI. Затова им трябва време, и то трябва да е ЕДНО число на едно
 * място, вместо шестнайсет преписани и НЕСЪГЛАСУВАНИ (до 13.09.2026 в
 * тестовете живееха три различни: 120_000 · 55_000 · 30_000).
 *
 * ЧИСЛОТО Е ИЗМЕРЕНО, не преценено на око (правило 14). На празна машина:
 * `chistota` 3412ms · `dalg` 2239ms · `belezi` 1299ms · `dnevnik` 1096ms.
 * Най-бавната е под три и половина секунди; 45 секунди ѝ дават над
 * ДЕСЕТОКРАТЕН запас — колкото ѝ трябва, когато vitest върти 82 работника и
 * всеки от тях иска нов Node процес.
 *
 * И стои ПОД таймаута на самия тест (60s в `vitest.config.ts`). Разликата не е
 * украса: щом машина зависне, `spawnSync` я прекъсва ПРЪВ и тестът казва КОЯ е
 * била. Обратният ред — какъвто беше досега — значи, че vitest отрязва целия
 * файл с „test timed out" и мълчи за причината. `tests/vremeto-na-portite.ts`
 * пази реда да не се обърне пак.
 */
export const VREME_NA_MASHINATA = 45_000;
