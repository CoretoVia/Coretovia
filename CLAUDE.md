# CLAUDE.md · правилата на Coretovia

**Дата:** 2026-09-05 · **Вид:** конституция · **Състояние:** жив

<!-- zadanieto-na-sesiyata -->
## 0 · ЗАДАНИЕТО НА СЕСИЯТА · за всеки агент и всяка сесия · пин ≤ 30 реда

Ти си в Coretovia. Собственикът решава, ти изпълняваш. Той е един човек и плаща за продукта, не за доклади за нас.

**ПРЕДИ ПЪРВИЯ РЕД**
1. `npm run nachalo` — състоянието идва оттам, не от паметта ти: следващият номер на коммит · последният ден · думите му · отворените редове · портите.
2. Кажи вида работа (У1–У10 · `docs/00-PROTOKOL.md`) и трите умения. Прочети задължителното за него ЦЯЛО.
3. Търси преди да питаш: `docs/registar-na-vaprosite.json` → `docs/izvori/dni/` → `zadanie/`. Въпрос с готов отговор е дефект (правило 22).

**ДОКАТО РАБОТИШ**
4. Негова дума → `docs/izvori/dni/ДНЕС.md` В МОМЕНТА, дословно, с час — преди отговора ти (правило 17 · 25).
5. Ново знание влиза само в дома на вида си. Четири форми: `ADR-NNN` · ден в `izvori/dni/` · ден в `dnevnik/` · именуван файл в папката на вида си. Никакъв нов `docs/NN`. Никакъв нов план, дълг или ред на четене — те имат по един дом.
6. Числата се броят с команда и командата стои до числото (правило 14).
7. Не кодираш без негова дума или обявено предположение (правило 26). Не строиш върху червено (правило 30).
8. Построено без жив викащ е дълг — ред в `docs/registar-na-dalga.json` (изгледът е `docs/14`) с машинно условие, не мълчание.
9. Избор пред него = форматът на правило 24, с тикчета.
10. Агентите четат; пишат само в `.rabotni/`. Не повече от три успоредни без негово ОК. Всеки агентски промпт започва с този раздел (`node stroezh/dnevnik.mjs --za-agenta`).

**ПРЕДИ ДА СПРЕШ**
11. Затворено → §3 на дневника с доказателство · родено → регистъра или дълга · следващото → §6.
12. `npm run kray` (от телефона: `npm run kray -- --bez-porti`). Коммит с номера от `nachalo`, на клон `hod-N-…` или `etap-N-…`, PR, `main` само през зелен CI.
13. Не знаеш къде отива нещо → питаш `docs/00-KARTA.md`, не измисляш дом.
14. Негова ръка (GitHub · Драйв · настройки) → СТЪПКИ, всяка с точното място: адрес · меню · бутон · какво трябва да види. Негово, 10.09: „да ми се подава задачите на стъпки с подробно описани за мястото".
<!-- /zadanieto-na-sesiyata -->

> ## ⇨ НОВ ЛИ СИ ТУК? ЗАПОЧНИ ОТ [`docs/00-KARTA.md`](docs/00-KARTA.md)
>
> Тя се ГЕНЕРИРА от дървото и от командите (`npm run karta`), а `npm run proverka`
> пада, ако е остаряла. Там е: къде си попаднал · редът на четене (четири стъпала) ·
> **всеки документ, описан от самия себе си** · броените числа · и машините, които
> пазят всичко това.
>
> **Питаш ли се дали нещо е решено — не питай човека, а виж
> `docs/registar-na-vaprosite.json`.** Всеки въпрос там носи едно от четири
> състояния и, ако е отговорен, НЕГОВИТЕ думи дословно и адреса им. Документ,
> който пита нещо вече отговорено, вали портата (`npm run registar`).
>
> **Цената, платена на 09.09.2026:** дългът държеше пет въпроса като „чака негова
> дума", а отговорите му стояха записани от предния ден на друго място. Негово:
> „**Не мога да говоря едни и същи неща всеки път.**"

Заданието е в `zadanie/` (Книгата, клетка по клетка). Решенията са в `docs/`. Този файл е
кратък: какво НЕ се нарушава. Наследник на MasterBook (VintexStroy, таг `alfa-kray-2026-09-05`);
правилата, платени там с находки, се пренасят, без наемите, ДДС, НАП и плановете.

## Езикът на кода

Имената в домейна са на латиница, но с българските думи от Книгата: `Vrata`, `Dnevnik`,
`Sabitie`, `Prozorets`, `Komanda`, `sverka`. Коментарите и съобщенията за грешки са на български.
Така кодът и Заданието говорят на един език и няма превод, в който да се губи смисъл.

## Трите нови правила на Coretovia

**К1 · Книгата е Заданието.** Всеки лист от неговия `.xlsx` е прозорец на програмата. Осем са —
„Само това и нищо повече или по малко" (05.09.2026). Имената им живеят САМО в
`src/model/osnova.ts`; нов прозорец няма команда и няма път (`tests/osemte.test.ts`).

**К2 · Портата е една.** UI-то, Книгата и агентите са три адаптера над ЕДИН порт
(`src/porta/`), а той — над един каталог от команди (`src/komandi/`). Единственият вход към
Вратата е каталогът. Посоката на зависимостите е една и я брои машина (`npm run sloeve`).

**К3 · Агентът не пише.** Той получава `PortaZaChetene` — тип без `izpalni`. Предлага чрез
`dryRun`, човекът приема, `actor` е човекът. Основните команди са замразен низ с контролна
сума — *обещание, не изпълнение:* контролна сума върху каталога ДНЕС НЯМА
(`tests/katalog.test.ts` пази само броя и формата на ключа) и това е **ДЛ-Т7**, отворен. Тук
се казва, защото правило 3 вече плати веднъж за правило, което обещава несъществуващо.

Негово, `zadanie/07-ii.md` B5, ДОСЛОВНО и ЦЯЛО: „**Никога не променя без да даде доклад и
отчет на промяната, освен ако не е автоматизирана от текущите задачи.**"

Това е липса на код, не промпт. **Изключението е негово и е част от изречението:**
автоматизираното от текущите задачи не иска отделен доклад всеки път — иначе всяка
автоматична стъпка би искала човек, и автоматизацията не би съществувала.

*(Дотук тук стоеше само „Никога не променя без доклад" — половин изречение, и то
преформулирано. Правило 17 беше нарушено в самата конституция, която го налага.)*

## Номерата на правилата · КОЕ ОТКЪДЕ Е

**Тук правилата са 33 плюс К1–К3. В MasterBook бяха 29 и с ДРУГА номерация** — а
по-старите ADR-и и част от `docs/` цитират НЕГОВИТЕ номера. Колко често — БРОИ СЕ,
не се преписва (правило 14); числото расте с всеки нов документ:

```bash
grep -ro "правило 17" docs/ | wc -l   # числото го казва командата · тук не стои
grep -ro "правило 29" docs/ | wc -l   # (10.09: стоящите тук числа бяха остарели петкратно
grep -ro "правило 21" docs/ | wc -l   #  — число, пришито към дата, остарява мълчаливо)
```

**СТАРОТО правило 29 (поверителността) тук е 21; ТУКАШНОТО 29 е друго — типизираното
поле.** Дотук тук пишеше „Правило 29 не съществува тук" — а трийсет реда по-надолу стои
`29. Полето е типизирано…`. Самопротиворечие в самата конституция, намерено на 14.09 по
негово искане (запис 232: „правилата… да няма грешки в тях"). Цитат на „правило 29" в
документ ПРЕДИ 08.09 значи поверителността; след това — полето. Датата на документа решава.
Ако документ цитира стар номер, чете се по таблицата:

| в текста пише | в MasterBook значи | ТУК е |
| :---- | :---- | :---- |
| правило 29 | поверителността | **21** |
| правило 25 | три умения на задача | **23** |
| правило 23 | правото само СТЕСНЯВА | **18** |
| правило 21 | цитат дословно | **17** |
| правило 20 | бутонът е път | **16** |
| правило 19 | данните не се превеждат | **15** |
| правило 18 | агентът не пише | **К3** |
| правило 17 | един факт, един дом | **14** |
| правило 16 | живото на чисто, старото в архива | **13** |
| правило 15 | изключено ≠ липсващо | **12** |
| правило 11 | латиница и кирилица | **10** |
| правило 10 | библиотека само срещу нерешен проблем | **9** |
| правила 1–8 | съвпадат | **1–8** |

**Не се пренномерира със заместване.** Няколко номера се сблъскват (старото 9
„замразен период" срещу тукашното 9 „библиотеките"; старото 22 срещу тукашното
22), тъй че сляпата замяна би сменила смисъла. Таблицата е ЕДИНСТВЕНИЯТ дом на
съответствието (правило 14); новите документи пишат ТУКАШНИТЕ номера.

## Правила, които не се нарушават (пренесени)

1. **Журналът е само за добавяне.** Поправка = ново събитие (сторно).
2. **Вратата е единственият вход за запис.** Нищо не пише в Журнала директно.
3. **Парите са цели центове.** Полетата завършват на `_st`; Вратата ги проверява; никакъв
   float. Валутите са две — евро и долар — избира се една при регистрация; курс няма.
   **Цените се закръглят на цяло НАГОРЕ, сметките на стотици към най-близкото, и закръгленото
   НИКОГА не влиза в сбор.**

   **ВТОРИЯТ НЕЗАВИСИМ ПЪТ Е ДОМЕЙНЕН, НЕ АРИТМЕТИЧЕН** — негово, 08.09: „**Парите минават
   по банка и по въведено. Двете са различни пътища, и банка и счетоводството се
   проверяват.**" Тоест сборът се пази от **преграда за цели числа** (число извън тях е
   отказ), а вторият път е **СВЕРКАТА банка ↔ въведено** (`docs/28` §3): движението по
   банка срещу декларираните фактури, кешът срещу трезора, картата срещу извлечението.

   *(Дотук тук пишеше „Всеки сбор има ВТОРИ, независим път (BigInt)". Такъв път НЯМА в
   живия код — функцията съществува и никой не я вика. Правилото обещаваше несъществуващо,
   а точно това наричаме дефект във всеки друг документ. Поправено с негова дума, 09.09,
   по правило 32: правило се мени само срещу довод за кода.)*
4. **Хеш-веригата не се заобикаля.** Подписът покрива всяко поле, включително `actor`.
5. **`opId` носи идемпотентността** — и носи ДЕЙСТВИЕТО (`komandaId#i`), не съдържанието.
6. **Всеки писач е своя верига.** Тактът е `ts · naematel · seq`; сблъсъците са находки за човек.
7. **Всяка партида завършва със сверка вход↔изход**, и разликата се записва дори когато е нула.
8. **При инцидент Журналът не се пипа.** Дърпа се кранът (`vrata.zatvori`).
9. **Библиотека влиза САМО срещу нерешен проблем** — през ADR с име · версия · лиценз · размер ·
   кой проблем; лиценз MIT/Apache-2.0/BSD/ISC (не GPL); в пакета при строеж; без мрежа при
   работа; парите остават наши (библиотека може да ПАРСВА, не да СМЯТА пари). Списъкът е пин с
   ръка в `tests/biblioteki.test.ts`; лицензите ги брои `npm run litsenzi`. Днес: `exceljs` от
   ЕДИН файл (`src/kniga/ooxml.ts`).
   **Негово, 07.09: „Ползвай библиотеки, които ще обхванат целия проект с малка бройка и
   код."** Тоест: предпочита се ЕДНА библиотека, която покрива цял отдел, пред три дребни;
   и се търси ГОТОВО решение, преди да се пише свое („няма да откриваш топлата вода").
   Библиотека за еднократно разследване живее в работната папка на сесията, ИЗВЪН
   хранилището, и не влиза в `package.json`.
10. **Латиница и кирилица не се смесват в една дума** (`tests/imena.test.ts`).
11. **Всичко в NFC на Вратата.**
12. **Изключено ≠ липсващо.** Отказът се КАЗВА с думи; недостижимата команда е сива и обяснява.
13. **Живото на чисто, старото в `docs/arhiv/`** с обяснението вътре (`tests/pravilo-16.test.ts`).
14. **Един факт, един дом.** Числата се БРОЯТ, не се преписват.
15. **Данните не се превеждат** — редовете с данни носят `translate="no"`.
16. **Бутонът е път, знакът е свойство.** Посоката е една (чете · смята · пише); сборът на
    парична колона отива сам в Приход (+) или Разход (−); знакът се смята, не се записва.
17. **Цитат се пренася ДОСЛОВНО или изобщо.** Съкращаване с многоточие — да; пренаписване вътре
    в кавичките — не.
18. **Правото само СТЕСНЯВА:** Редактира · Вижда · Скрито; най-тясното печели; колона се СКРИВА,
    ред се ИЗКЛЮЧВА; скритото пак се смята. Достъпът е по ЧЕТИРИ оси (табове · хедъри · редове ·
    Журнал), от Длъжността.
19. **Меню, което ОПИСВА → расте свободно. Меню, върху което системата СМЯТА → расте само от
    Настройки.**
20. **Сметки е пясъчникът, Управление е истината, и между тях стои ЧОВЕК.**
21. **Поверителните данни на всеки живеят в НЕГОВОТО пространство** — устройството и Драйвът
    му — и не се споделят в пространството на друг: служителят не влиза в служебното, затова
    неговият Журнал стои на неговия личен облак. Книгата на Стопанина не влиза в публичното
    хранилище в GitHub; влиза генерирана мостра (`tests/poveritelnost.test.ts`).

    *(Дотук тук пишеше „никога при нас" — и „нас" се четеше като „ИИ да не чете Книгата на
    Стопанина". Негово, 14.09 (запис 232), ДОСЛОВНО: „правилото се отнася само до следващи
    хранилища различни от това на Стопанина… Не може служителя да ползва служебното
    пространство, за това дава достъп неговия журнал да си е на личен облак." Правилото е за
    РАЗДЕЛЕНИЕТО между хора, не за четеца. Сменено по правило 32 с тази му дума.)*

    И втората му дума същия ден, 20:43 (запис 234), ДОСЛОВНО: „за сега нека дава позволение за
    четене, те щае са на един драйв. Дови нека правилото важи само за стопаните." Тоест
    четенето в неговото пространство е ПОЗВОЛЕНО, и правилото пази СТОПАНИТЕ — обхватът му
    при Длъжностите се решава в ход 4 (правото), не тук.
22. **Търсиш преди да питаш:** настоящите истини → `zadanie/` → старото хранилище → справка навън →
    чак тогава въпрос, и той казва какво е претърсено.
    **Цената, платена на 07.09:** попитан беше „Гантът картина ли е или граф", а
    `zadanie/03` и `zadanie/04` вече казваха **„Диаграма Гант (Календар)"** — отговорът
    стоеше в Заданието, написан от него. Негово: „Дал съм ви всичко, а вие задавате едни и
    същи глупави въпроси." Въпрос, чийто отговор е в извора, е ДЕФЕКТ, не любезност.
23. **Три умения на задача**, назовани поименно преди началото — и **КАКВО СЕ ЧЕТЕ за
    всяко, е в `docs/00-PROTOKOL.md`**, пазен от `npm run protokol`.

    **Цената, платена на 10.09.2026:** негово — „**Няма протокол какво да се чете като
    умения, за да няма пропуски?**" Нямаше. Това правило стоеше тук от самото начало и
    **нито една машина не го гледаше**; ден по-рано негова дума беше извадена извън
    обхвата си и оправда изтриване на Журнала. Изречение без машина е пожелание.

    Протоколът има **седем вида работа**, всеки със ЗАДЪЛЖИТЕЛНО ЧЕТЕНЕ, с **платената
    цена**, която го е родила, и с „как се познава, че е спазено". Вид работа, който го
    няма там, е **находка, не свобода**.
24. **ФОРМАТЪТ НА СРАВНЕНИЕТО е задължителен, не по избор.** Всеки въпрос с избор се
    връща на собственика САМО така, и никога като свободен текст:

    | какво | иска се |
    | :---- | :---- |
    | **редове** | ТЕМИТЕ (сигурност · данни · код в редове · цена · закон · обещание пред клиента · обратимост) |
    | **колони** | вариантите, два до четири |
    | **всяка пресечка** | предимство и недостатък, конкретно |
    | **накрая на всяка тема** | кой вариант печели ПО ТАЗИ ТЕМА |
    | **кой го ползва** | назовани компании или проекти, ПРОВЕРЕНИ · измислен пример е по-лош от липсващ |
    | **цена** | редове код · часове · и колко струва, АКО СГРЕШИМ |
    | **накрая** | ЕДНА препоръка, обоснована с крайния продукт, не с вкус |

    Причина: искано е четири пъти (05.09 · 06.09 два пъти · 07.09) и три пъти е пропуснато.
    Правилото съществува, за да не се моли форматът, а да се дължи.
25. **Отговорите се СЪБИРАТ, не се разпиляват.** Всяка негова дума влиза В МОМЕНТА,
    дословно и с час, в деня си — `docs/izvori/dni/ГГГГ-ММ-ДД.md` — и се маха от списъка
    с отворени. Въпрос, зададен втори път, е дефект — значи първият отговор не е записан.
    *(До 09.09.2026 домът беше `docs/10-kakvo-chaka.md`; той остава като запис, записи
    1–104, и не расте. Сменено на 10.09 по правило 32 с негов избор — „docs/izvori/dni" —
    защото думите му живееха на девет места, а `docs/10` замръзна при пет коммита за ден.
    `npm run dnevnik:proveri` пада, ако денят няма файл или файлът е празен.)*
26. **НЕ СЕ КОДИРА БЕЗ НЕГО.** Ред код не се пише, преди фундаменталните въпроси на този
    резен да имат негов отговор или ОБЯВЕНО предположение. Редът е един и не се обръща:

        дълбае се  →  сравнява се в ТАБЛИЦА  →  оценява се  →  той решава  →  чак тогава код

    „Работа на парче" и „кодиране на парче" са ДЕФЕКТ, не стил. Резен, започнат без
    отговор, е резен, който ще се пренаписва — а Журналът е само за добавяне и не прощава.
    Негово, 07.09: „Няма да кодираш без мен."

27. **КРИТИЧНОСТТА Е ЗАДЪЛЖЕНИЕ, не любезност.** Търси се най-доброто за кода, не
    най-приятното за ухото. Съгласие без проверка е по-скъпо от спор: всяка от двете
    сверки на 06–07.09 обори собствените си препоръки — тринайсет от тринайсет скептика
    при първата, четиринайсет твърдения при втората, три от тях в мои живи документи.
    Препоръка, която никой не е оборвал, още не е препоръка.

28. **ВСИЧКО Е ЕКСЕЛ.** Негово, 07.09: „Основата на всичко е една екселска таблица и да
    се превърне в приложение… всичко е таблици и формули в основата на скелета."
    Всеки екран се пита ДВЕ неща, преди да се напише ред: **коя таблица е това** и **коя
    формула я смята**. Няма изглед, който да не е таблица отдолу. Гантът е КАЛЕНДАР с
    цифри и дати (`zadanie/03` · `zadanie/04`), не картина.

29. **Полето е типизирано и само казва дали се попълва.** Негово, 07.09: „полетата за
    бутони и полета за цифри, една част са само за наблюдение, не за попълване, да са
    равни и типизирани като в таблица." Тоест всяко поле носи вида си и правото си:
    попълва се · само се гледа (ЗАТВОРЕНА колона, ADR-010) · смята се. Затвореното пак
    се смята (правило 18). Равни значи еднакви по форма през целия продукт.

30. **НИЩО НЕ ЧАКА БЕЗ ОТПУШВАЩО УСЛОВИЕ · и назад не остава недовършено.** Негово, 08.09:
    „да не се движи напред, докато нещо назад остава недовършено или нерешено… да не се
    оставя нищо в кода за време, което пречи и създава проблеми."

    И поправката му веднага след това: **„Искам да не определяш дни, а да работиш по план
    и колкото време отнеме… не се строи, ако не е здраво и не издържа проверките и
    тестовете."** Датата е ПРЕДПОЛОЖЕНИЕ и остарява мълчаливо; условието е ФАКТ и се
    проверява. Затова всеки ред в дълга носи не срок, а **какво трябва да е готово преди
    него** и **как се познава, че е затворен**.

    Три забрани, всяка платена с находка:

    · **Построено и невикано е ДЪЛГ, не възможност.** Одитът на 08.09 намери един и същи
      клас пет пъти за 48 часа (котвата · `sabiri` · `zapis.ts` · `verigaLichna` ·
      `vazstanovi`). Зелен тест над код без викащ ИЗГЛЕЖДА като гаранция и точно затова
      е по-опасен от липсващ код.
    · **Всяко чакащо нещо носи МЯСТО и ОТПУШВАЩО УСЛОВИЕ.** „После" не е план. Пише се в
      `docs/registar-na-dalga.json` (от 11.09 · `docs/14-dalgat.md` се генерира оттам) с ред:
      какво · къде · **какво трябва преди него** · **машинното условие, по което се познава,
      че е затворен** (`npm run dalg` го проверява и вади „вече затворен" и „отворил се е тихо").
    · **Не се строи върху нездраво.** Ход не се смята за затворен, докато деветте порти не
      са зелени и проходът не е минал два пъти. Ред, който „почти" минава, е отворен ред.
    · **Напред не се тръгва с недовършено назад.** Нов ход не почва, преди предишният да е
      затворен или изрично отложен с ОТПУШВАЩО УСЛОВИЕ. *(До 11.09 тук пишеше „С ДАТА" —
      срещу собствената му дума по-горе, „не определяй дни"; сменено по правило 32, доводът е
      в дневника на 11.09.)*

    Домът на чакащото е ЕДИН (правило 14): `docs/registar-na-dalga.json`, четен като
    `docs/14-dalgat.md`. **Ред без отпушващо условие е дефект** — той е „после", преоблечено
    като план; редовете без машинно условие се броят и броят им може само да пада.

31. **ХЕЛПЪТ СЕ ОБНОВЯВА С КОДА.** Негово, 08.09: „Разработи ги накрая на всяка тема.
    **Ъпдейтва се Хелпа с всяка промяна на кода.**"

    **Обяснението живее ПРИ нещото**, не в отделен панел: подсказка при задържане на
    мишката (`title`), върху самото поле, бутон или глава. Негово, 08.09: „**В сегашния
    хелп са дадени добре при задържане на мишката — така се прави, старото е по-лошо.**"
    Панелът от MasterBook (`ADR-136`) НЕ се пренася — собственият му §3 признава защо:
    „предупреждение в панел, който човек не е отворил, не предупреждава никого".

    **Подсказката казва ФОРМУЛАТА** (правило 28), не името: „площ × база по вид" ·
    „земя + строителна − овехтяване" · „очакван наем ÷ доходност". Сивият бутон казва
    ЗАЩО е сив (правило 12).

    **Две степени:** **Начало** (подробното · защо съществува полето + формулата с
    думи) и **Нормален** (съкратеното · само формулата). Превключват се от Настройки
    и от екрана; подробното стои и върху ИМЕТО на програмата (после логото) и се
    сменя с таба — негово, 11.09: „когато таба се сменя задържайки на името да се
    показва подробната инфортмация". Подсказките са с НАШИ думи; неговите изречения
    от Книгата са извор, не екран (негово, 10.09: „с твои думи").

    *(Дотук тук стоеше „Експерт" — негово от 08.09 вечерта, запис 60. Сменено на
    11.09 в ход Х по правило 32 с по-късната му дума, 10.09 21:11, запис 132:
    „…**подробна информация при избор на начален HELP и съкратена информация при
    нормален HELP**" — и същата дума от 08.09 сутринта, запис 19: „Начален хелп и
    нормален." Доводът за кода: в кода и в паметта на екрана живее латински ключ
    `nachalo · normalno`, етикетът е на едно място и се сменя без миграция.)*

    **Ход не е затворен, докато хелпът мълчи за онова, което ходът е добавил.** Хелп,
    който изостава от кода, лъже за програмата — същият клас дефект, който
    `tests/obeshtaniyata.test.ts` вече лови.

32. **ПРАВИЛО СЕ МЕНИ САМО СРЕЩУ ДОВОД ЗА КОДА.** Негово, 08.09, дадено като мандат:
    „**Мени се, ако е най-добре за кода и неговата функционална, надеждна и чиста работа.
    Само това е водещо условие — с модели от модерната водеща практика.**"

    Тоест: правило се мени, когато промяната **доказано** служи на кода, и доводът сочи
    **назована и проверена** практика (правило 24: измислен образец е по-лош от липсващ).
    Промяната се ЗАПИСВА — какво, защо, и кой го прави така. **Тиха промяна на правило е
    дефект.** Мандатът не е разрешение за вкус; той е задължение за довод.

33. **ЕВТИНО И ВИДИМО.** Негово указание, 11.09.2026 — документ „Claude Code във VS Code"
    (`.rabotni/ukazanie-claude-code-vs-code.pdf`), даден след ден, в който агенти изядоха
    дневния му лимит без нищо на екрана. Осем неща, всяко машинно или проверимо:

    · **Всяка стъпка завършва с нещо ВИДИМО в програмата**, не с документ. Документът
      придружава екрана; не го замества.
    · **Преди всяко действие — кратко резюме какво ще стане**, с НАШИ думи. Негово,
      11.09: „**какво ще направиш всеки път преди да го направиш подробно резюме**".
    · **Подагентът се избира по цена:** четене и търсене → `chetets` (Haiku) · преглед →
      `skeptik` (Sonnet) · писане на код → Opus на effort high. Скъп модел за дребна
      работа е разхищение, назовано в самото указание.
    · **Моделът и усилието не се сменят по средата на задача** — анулират кеша.
    · **Всеки промпт носи КРИТЕРИЙ ЗА УСПЕХ** („готово е, когато X минава и числото е Y").
    · **След ДВА неуспешни опита се спира**, наученото се записва, контекстът се чисти.
      Трети опит върху два провала е по-лош от нов старт с бележките.
    · **„Да започнем наново" не се приема** — иска се списък с грешки по файл и ред.
    · **Личните му данни не влизат в СПОДЕЛЕНОТО:** `.claude/settings.json` пази `.env`
      от четене в сесия — за да не попадне по невнимание в коммит, в доклад или в чужд
      контекст. Това е предпазител на ХРАНИЛИЩЕТО, не правило, че ИИ, който работи за
      Стопанина, не бива да вижда неговите данни (правило 21, както той го уточни на 14.09).
      **Забраната за `kniga-lichna/` е МАХНАТА на 14.09 по негово решение** (запис 234:
      „за сега нека дава позволение за четене") и **се връща във финалната версия за
      маркетинг** — ДЛ-Т67, с машинно условие. Книгата му пак не влиза в хранилището
      (`tests/poveritelnost.test.ts` — това не е сменяно).

    Подагентите живеят в `.claude/agents/`; настройките и забраните — в
    `.claude/settings.json`. Те са В ХРАНИЛИЩЕТО, за да важат за всеки, който влезе тук,
    а не в паметта на един агент.

## Ред на работа

Ходове (`docs/03-plan.md` §1 · един план, един дом), стъпили върху затворените резени на
основата (§2). Един ход = работеща функция от край до край + тест + проход + сверка + хелп
за добавеното (правило 31). Следващият не почва, преди предишният да е затворен или изрично
отложен с отпушващо условие (правило 30). Резенът беше единицата до 10.09; ходът — оттогава.

## Началото и краят на сесията · и преди всеки коммит

```bash
npm run nachalo    # НАЧАЛОТО · състоянието · следващият номер · дневникът на деня се отваря
npm run proverka   # портите · списъкът и броят им са в package.json, не тук (правило 14)
npm run proba:dva  # проходът през истински браузър · два пъти поред
npm run kray       # КРАЯТ · пуска горните две · §5 на дневника · картата · README · после проверява
```

Куките в `stroezh/kuki/` (закачени от `npm install` през `prepare`) пускат БЪРЗОТО пред
всеки коммит — дневник · регистър · протокол · карта · белези · дълг · стил върху подготвеното — и
проверяват номера в заглавието: „N · …", където N е следващият свободен. Пълното е
`kray` и CI; `docs/14` §4 отдавна е решил, че проходът не се пуска пред всеки коммит.

Червен тест не се изключва и не се маркира като „ще го оправим после". Числата долу се
БРОЯТ от изхода на самите команди (правило 14), не се преписват:

`chistota` брои **13 обхода** — СЕДЕМ с праг нула (мъртво · излишен `export` · празно поле ·
излишни редове · несвързан · път до HTML извън вратата · шум), ДВА с праг над нулата
(дублирано 1 · дублирано по структура 2), ТРИ с ТОЧЕН пин (само тест · само през барела ·
без тест — от 11.09, ход 9: нагоре е невикано без ред в дълга, надолу е пин за сваляне) и
ЕДИН брояч без праг (изнесено за теста).
`chestnost` брои **12 обхода** (А · Б · В · Г · Д · Е · Ж · З · И · Й · К · Л), всичките на нула —
бяха ПЕТ, ADR-015 добави шест и числото тук стоя невярно до 08.09.2026. Мострата (`npm run mostra`) не се вика от строежа — сменя се с ревю.

## Редът на четене

`docs/00-CHETI-PARVO.md` е входната врата. Приключеното е в `docs/arhiv/`.
